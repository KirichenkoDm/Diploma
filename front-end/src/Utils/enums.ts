export enum Roles {
  admin = "admin",
  student = "student",
  teacher = "teacher",
  guest = "guest",
}

export enum Topics {
  buisness = "Buisness",
  marketing = "Marketing",
  art = "Art",
  IT = "IT",
  selfDevelopment = "Self Development",
}

export enum FetchStatus {
  idle = "idle",
  loading = "loading",
  succeeded = "succeeded",
  failed = "failed",
}
